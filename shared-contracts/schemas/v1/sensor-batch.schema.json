{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://gymcoach.local/contracts/watch/v1/sensor-batch.schema.json",
  "title": "SensorBatch",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "protocolVersion",
    "schemaVersion",
    "batchId",
    "sessionId",
    "source",
    "deviceId",
    "createdAt",
    "sequence",
    "totalSequences",
    "sampleCount",
    "samples"
  ],
  "properties": {
    "protocolVersion": { "type": "string", "const": "1.0" },
    "schemaVersion": { "type": "integer", "const": 1 },
    "batchId": { "type": "string", "format": "uuid" },
    "sessionId": { "type": "string", "minLength": 1, "maxLength": 128 },
    "source": { "type": "string", "enum": ["PHONE", "WATCH"] },
    "deviceId": { "type": "string", "minLength": 1, "maxLength": 128 },
    "createdAt": { "type": "integer", "minimum": 0 },
    "sequence": { "type": "integer", "minimum": 1 },
    "totalSequences": { "type": "integer", "minimum": 1 },
    "sampleCount": { "type": "integer", "minimum": 1 },
    "samples": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "sensor-sample.schema.json" }
    }
  }
}
