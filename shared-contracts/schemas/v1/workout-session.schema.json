{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://gymcoach.local/contracts/watch/v1/workout-session.schema.json",
  "title": "WorkoutSession",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "sessionId",
    "workoutProgramId",
    "userId",
    "status",
    "startedAt",
    "finishedAt",
    "activeExerciseId",
    "activeSetId",
    "revision",
    "updatedAt",
    "updatedBy"
  ],
  "properties": {
    "sessionId": { "type": "string", "minLength": 1, "maxLength": 128 },
    "workoutProgramId": { "type": "string", "minLength": 1, "maxLength": 128 },
    "userId": { "type": "string", "minLength": 1, "maxLength": 128 },
    "status": { "type": "string", "enum": ["ACTIVE", "PAUSED", "FINISHED"] },
    "startedAt": { "type": "integer", "minimum": 0 },
    "finishedAt": { "type": ["integer", "null"], "minimum": 0 },
    "activeExerciseId": {
      "type": ["string", "null"],
      "minLength": 1,
      "maxLength": 128
    },
    "activeSetId": {
      "type": ["string", "null"],
      "minLength": 1,
      "maxLength": 128
    },
    "revision": { "type": "integer", "minimum": 1 },
    "updatedAt": { "type": "integer", "minimum": 0 },
    "updatedBy": { "type": "string", "enum": ["PHONE", "WATCH"] }
  }
}
